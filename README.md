# Bitrix Stubs Generator

Позволяет сгенерировать стабы для использования их стат. анализом.

# Command Line Usage

Чтобы сгенеировать стабы: 
`./vendor/bin/console stubs:generate [root] -m [модуль1] -m [модуль2]`
